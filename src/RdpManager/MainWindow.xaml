<ui:FluentWindow x:Class="RdpManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Waypoint" Width="1180" Height="720" MinWidth="900" MinHeight="560"
        Icon="pack://application:,,,/Assets/app.ico"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        Loaded="Window_Loaded" KeyDown="Window_KeyDown" PreviewMouseWheel="Window_PreviewMouseWheel" Closing="Window_Closing" StateChanged="Window_StateChanged">

    <Window.Resources>
        <!-- Kolory / pędzle / gradienty / font żyją w Themes/Palette.{Dark,Light}.xaml
             (na poziomie App, przełączane w runtime przez ThemeManager). Tu tylko style. -->

        <!-- Rail button (icon) -->
        <Style x:Key="RailBtn" TargetType="Button">
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextTer}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource RailHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="RailBtnActive" TargetType="Button" BasedOn="{StaticResource RailBtn}">
            <Setter Property="Background" Value="{DynamicResource AccentSoft}"/>
        </Style>

        <!-- 28x28 icon button (status bar) -->
        <Style x:Key="IconBtn" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="3,0,0,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource Elevated}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextSec}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Panel}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="18,14"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>
        <Style x:Key="CardHead" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextPrim}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="AppTitleBar" Grid.Row="0" Height="32" Title="Waypoint">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Desktop24" Foreground="{DynamicResource Accent}"/>
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <!-- BODY (skalowane zoomem Ctrl+kółko; titlebar zostaje 1:1) -->
        <Grid x:Name="BodyGrid" Grid.Row="1">
            <Grid.LayoutTransform>
                <ScaleTransform x:Name="RootScale" ScaleX="1" ScaleY="1"/>
            </Grid.LayoutTransform>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- RAIL -->
            <Border x:Name="Rail" Grid.Column="0" Width="52" Background="{DynamicResource RailBg}"
                    BorderBrush="{DynamicResource Border}" BorderThickness="0,0,1,0">
                <Grid Margin="0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" HorizontalAlignment="Center">
                        <Button x:Name="NavDashboard" Style="{DynamicResource RailBtn}" ToolTip="{DynamicResource S.nav.dashboard}" Tag="Dashboard" Click="Nav_Click">
                            <ui:SymbolIcon x:Name="IcoDashboard" Symbol="Apps24" FontSize="18" Foreground="{DynamicResource TextTer}"/>
                        </Button>
                        <Button x:Name="NavSessions" Style="{DynamicResource RailBtn}" ToolTip="{DynamicResource S.nav.sessions}" Tag="Sessions" Click="Nav_Click">
                            <ui:SymbolIcon x:Name="IcoSessions" Symbol="Desktop24" FontSize="18" Foreground="{DynamicResource TextTer}"/>
                        </Button>
                        <Button x:Name="NavRecent" Style="{DynamicResource RailBtn}" ToolTip="{DynamicResource S.nav.recent}" Tag="Recent" Click="Nav_Click">
                            <ui:SymbolIcon x:Name="IcoRecent" Symbol="History24" FontSize="18" Foreground="{DynamicResource TextTer}"/>
                        </Button>
                        <Button x:Name="NavTools" Style="{DynamicResource RailBtn}" ToolTip="{DynamicResource S.nav.tools}" Click="OpenPasswordGen_Click">
                            <ui:SymbolIcon x:Name="IcoTools" Symbol="Key24" FontSize="18" Foreground="{DynamicResource TextTer}"/>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Row="2" HorizontalAlignment="Center">
                        <Button x:Name="AvatarBtn" Style="{DynamicResource RailBtn}" ToolTip="{DynamicResource S.nav.about}" Click="Avatar_Click">
                            <ui:SymbolIcon Symbol="Info24" FontSize="18" Foreground="{DynamicResource TextTer}"/>
                        </Button>
                        <Button x:Name="NavSettings" Style="{DynamicResource RailBtn}" ToolTip="{DynamicResource S.nav.settings}" Tag="Settings" Click="Nav_Click">
                            <ui:SymbolIcon x:Name="IcoSettings" Symbol="Settings24" FontSize="17" Foreground="{DynamicResource TextTer}"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- SIDEBAR -->
            <Border x:Name="Sidebar" Grid.Column="1" Width="228" Background="{DynamicResource Panel}"
                    BorderBrush="{DynamicResource Border}" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ui:TextBox x:Name="SearchBox" Grid.Row="0" Margin="12" PlaceholderText="{DynamicResource S.search}"
                                TextChanged="SearchBox_TextChanged">
                        <ui:TextBox.Icon>
                            <ui:SymbolIcon Symbol="Search24"/>
                        </ui:TextBox.Icon>
                    </ui:TextBox>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="8,0">
                        <AdornerDecorator>
                            <StackPanel x:Name="ServerTree"/>
                        </AdornerDecorator>
                    </ScrollViewer>

                    <Border Grid.Row="2" BorderBrush="{DynamicResource Border}" BorderThickness="0,1,0,0" Padding="10">
                        <StackPanel>
                            <!-- Pojawia się tylko, gdy CheckForUpdatesAsync znajdzie nowszą wersję. -->
                            <ui:Button x:Name="UpdateBtn" Visibility="Collapsed" HorizontalAlignment="Stretch"
                                       Appearance="Info" Margin="0,0,0,6" Click="Update_Click"/>
                            <ui:Button Content="{DynamicResource S.quickConnect}" HorizontalAlignment="Stretch"
                                       Appearance="Primary" Click="QuickConnect_Click"
                                       ToolTip="{DynamicResource S.quickConnect.tip}"/>
                            <ui:Button Content="{DynamicResource S.addServer}" HorizontalAlignment="Stretch"
                                       Appearance="Secondary" Margin="0,6,0,0" Click="AddServer_Click"/>
                            <!-- Jeden przycisk importu z menu źródeł (mstsc / .rdp / mRemoteNG / RDCMan). -->
                            <ui:Button Content="{DynamicResource S.import}" HorizontalAlignment="Stretch"
                                       Appearance="Secondary" Margin="0,6,0,0" Click="ImportMenu_Click">
                                <ui:Button.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="{DynamicResource S.import.mstsc}" Click="ImportMstsc_Click"
                                                  ToolTip="{DynamicResource S.importMstsc.tip}"/>
                                        <MenuItem Header="{DynamicResource S.import.rdpfile}" Click="ImportRdp_Click"/>
                                        <MenuItem Header="{DynamicResource S.import.mrng}" Click="ImportMrng_Click"/>
                                        <MenuItem Header="{DynamicResource S.import.rdcman}" Click="ImportRdg_Click"/>
                                        <MenuItem Header="{DynamicResource S.import.rdm}" Click="ImportRdm_Click"/>
                                    </ContextMenu>
                                </ui:Button.ContextMenu>
                            </ui:Button>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- MAIN (przełączane widoki: Sesje / Pulpit / Ostatnie / Ustawienia) -->
            <Grid Grid.Column="2">
            <Grid x:Name="SessionsView">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <!-- Strefa „hot zone" pod paskiem tylko dla kotwicy paska pełnoekranowego (Popup).
                         W oknie 0 (żeby nie było odstępu pod kartami); w pełnym ekranie i tak jest 0. -->
                    <RowDefinition x:Name="SessionHotZoneRow" Height="0"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- tab strip (built in code) -->
                <!-- IsHitTestVisibleInChrome: w trybie skupienia titlebar znika, a pasek kart wjeżdża
                     w strefę caption WindowChrome — bez tego kliknięcia w karty przeciągałyby okno. -->
                <Border x:Name="TabStripHost" Grid.Row="0" shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Background="{DynamicResource RailBg}"
                        BorderBrush="{DynamicResource Border}" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <!-- Pasek przewijania ukryty: przy wielu kartach przewija się kółkiem myszy (handler niżej)
                             i przeciąganiem — bez brzydkiego natywnego scrollbara pod kartami. -->
                        <ScrollViewer x:Name="TabScroller" Grid.Column="0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                                      PreviewMouseWheel="TabScroller_PreviewMouseWheel">
                            <StackPanel x:Name="TabStrip" Orientation="Horizontal" Margin="8,6,8,6"/>
                        </ScrollViewer>
                        <!-- Tryb skupienia (zmaksymalizowane): titlebar + pasek stanu znikają, więc pełny ekran
                             i przyciski okna (minimalizuj / przywróć / zamknij) mieszkają tu, na pasku kart. -->
                        <!-- IsHitTestVisibleInChrome na KAŻDYM przycisku: bez tego środek ikony (SymbolIcon)
                             ląduje w strefie caption i hover/klik nie łapie (działa tylko brzeg). -->
                        <StackPanel x:Name="FocusControls" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"
                                    Margin="4,0,6,0" Visibility="Collapsed">
                            <Button x:Name="QuickSwitchBtn" Style="{DynamicResource IconBtn}" Click="QuickSwitch_Click"
                                    shell:WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="{DynamicResource S.qs.tip}">
                                <ui:SymbolIcon Symbol="Add24" FontSize="15" Foreground="{DynamicResource TextTer}"/>
                            </Button>
                            <Button Style="{DynamicResource IconBtn}" Click="ToggleFocus_Click"
                                    shell:WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="{DynamicResource S.focus.toggle}">
                                <TextBlock Text="&#x25A3;" FontSize="15" Foreground="{DynamicResource TextTer}" VerticalAlignment="Center"/>
                            </Button>
                            <Button Style="{DynamicResource IconBtn}" Click="Fullscreen_Click"
                                    shell:WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="{DynamicResource S.tip.fullscreen}">
                                <ui:SymbolIcon Symbol="FullScreenMaximize24" FontSize="15" Foreground="{DynamicResource TextTer}"/>
                            </Button>
                            <Button Style="{DynamicResource IconBtn}" Click="FocusMinimize_Click"
                                    shell:WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="{DynamicResource S.tip.minimize}">
                                <TextBlock Text="&#x2212;" FontSize="15" Foreground="{DynamicResource TextSec}" VerticalAlignment="Center"/>
                            </Button>
                            <Button Style="{DynamicResource IconBtn}" Click="FocusRestore_Click"
                                    shell:WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="{DynamicResource S.fs.restore}">
                                <TextBlock Text="&#x2750;" FontSize="13" Foreground="{DynamicResource TextSec}" VerticalAlignment="Center"/>
                            </Button>
                            <Button Style="{DynamicResource IconBtn}" Click="FocusClose_Click"
                                    shell:WindowChrome.IsHitTestVisibleInChrome="True" ToolTip="{DynamicResource S.m.close}">
                                <TextBlock Text="&#x2715;" FontSize="13" Foreground="{DynamicResource Danger}" VerticalAlignment="Center"/>
                            </Button>
                        </StackPanel>
                        <!-- Połączona sesja (poza trybem skupienia): akcje przy prawym końcu paska kart —
                             zamiast osobnego paska stanu. Widoczne tylko gdy okno NIE jest w trybie skupienia,
                             więc pasek kart nie leży w strefie caption i nie trzeba tu IsHitTestVisibleInChrome. -->
                        <StackPanel x:Name="SessionActions" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"
                                    Margin="4,0,6,0" Visibility="Collapsed">
                            <Button x:Name="FilesBtn" Style="{DynamicResource IconBtn}" Click="Files_Click"
                                    Visibility="Collapsed" ToolTip="{DynamicResource S.sftp.toggle}">
                                <ui:SymbolIcon Symbol="Folder24" FontSize="15" Foreground="{DynamicResource TextTer}"/>
                            </Button>
                            <Button Style="{DynamicResource IconBtn}" Click="ToggleFocus_Click" ToolTip="{DynamicResource S.focus.toggle}">
                                <TextBlock Text="&#x25A3;" FontSize="15" Foreground="{DynamicResource TextTer}" VerticalAlignment="Center"/>
                            </Button>
                            <Button Style="{DynamicResource IconBtn}" Click="Fullscreen_Click" ToolTip="{DynamicResource S.tip.fullscreen}">
                                <ui:SymbolIcon Symbol="FullScreenMaximize24" FontSize="15" Foreground="{DynamicResource TextTer}"/>
                            </Button>
                            <Button Style="{DynamicResource IconBtn}" Click="Disconnect_Click" ToolTip="{DynamicResource S.disconnect}">
                                <TextBlock Text="✕" FontSize="14" Foreground="{DynamicResource Danger}"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- session toolbar: two states -->
                <Border x:Name="SessionToolbar" Grid.Row="1" Background="{DynamicResource Panel}"
                        BorderBrush="{DynamicResource Border}" BorderThickness="0,0,0,1" Padding="12,5">
                    <Grid>
                        <!-- disconnected -> kompaktowy pasek połączenia (pełna edycja jest w oknie serwera) -->
                        <StackPanel x:Name="ConnectForm" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="CfAvatar" Width="18" Height="18" CornerRadius="5" VerticalAlignment="Center" Margin="0,0,8,0">
                                <TextBlock x:Name="CfAvatarText" Foreground="White" FontSize="7.5" FontWeight="Bold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock x:Name="CfName" Foreground="{DynamicResource TextPrim}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBlock x:Name="CfHost" FontFamily="{DynamicResource Mono}" Foreground="{DynamicResource TextTer}"
                                       VerticalAlignment="Center" Margin="10,0,0,0"/>
                            <CheckBox x:Name="WinAuthCheck" Content="{DynamicResource S.winauth}" Foreground="{DynamicResource TextSec}"
                                      VerticalAlignment="Center" Margin="16,0,0,0" Checked="WinAuth_Changed" Unchecked="WinAuth_Changed"/>
                            <StackPanel x:Name="CfPassGroup" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                                <TextBlock Text="{DynamicResource S.pass}" Style="{DynamicResource FieldLabel}"/>
                                <PasswordBox x:Name="PassBox" Width="150" VerticalContentAlignment="Center" KeyDown="PassBox_KeyDown"/>
                            </StackPanel>
                            <ui:Button x:Name="ConnectBtn" Content="{DynamicResource S.connect}" Appearance="Primary" Click="Connect_Click" Margin="16,0,0,0"/>
                            <Ellipse x:Name="CfStatusDot" Width="7" Height="7" Fill="{DynamicResource TextTer}"
                                     VerticalAlignment="Center" Margin="14,0,6,0" Visibility="Collapsed"/>
                            <TextBlock x:Name="StatusText" Text="—" Foreground="{DynamicResource TextSec}" VerticalAlignment="Center"
                                       MaxWidth="460" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>

                    </Grid>
                </Border>

                <Border x:Name="HotZone" Grid.Row="2" Background="Transparent"/>

                <!-- all sessions' hosts live here; only the active one is Visible -->
                <Grid x:Name="SessionContainer" Grid.Row="3" Background="{DynamicResource CanvasBrush}">
                    <TextBlock x:Name="EmptyHint" Text="{DynamicResource S.emptyhint}"
                               Foreground="{DynamicResource TextTer}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                    <!-- Stan sesji gdy kontrolka RDP jest schowana (łączenie / rozłączono) -->
                    <Border x:Name="SessionOverlay" Visibility="Collapsed" Background="{DynamicResource CanvasBrush}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="460">
                            <ui:ProgressRing x:Name="OverlaySpinner" IsIndeterminate="True" Width="46" Height="46"
                                             Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,0,0,18"/>
                            <TextBlock x:Name="OverlayTitle" Foreground="{DynamicResource TextPrim}" FontSize="16"
                                       FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="OverlayMsg" Foreground="{DynamicResource TextSec}" HorizontalAlignment="Center"
                                       TextAlignment="Center" TextWrapping="Wrap" Margin="0,8,0,0"/>
                            <ui:Button x:Name="OverlayReconnect" Content="Połącz ponownie" Appearance="Primary"
                                       Click="OverlayAction_Click" HorizontalAlignment="Center" Margin="0,18,0,0" Visibility="Collapsed"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
            <!-- /SessionsView -->

            <!-- Pulpit -->
            <ScrollViewer x:Name="DashboardView" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" Background="{DynamicResource CanvasBrush}">
                <StackPanel Margin="28,24">
                    <TextBlock Text="{DynamicResource S.nav.dashboard}" Foreground="{DynamicResource TextPrim}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,18"/>
                    <StackPanel x:Name="DashboardPanel"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Ostatnie -->
            <ScrollViewer x:Name="RecentView" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" Background="{DynamicResource CanvasBrush}">
                <StackPanel Margin="28,24">
                    <TextBlock Text="{DynamicResource S.view.recent}" Foreground="{DynamicResource TextPrim}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,18"/>
                    <StackPanel x:Name="RecentPanel"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Ustawienia -->
            <ScrollViewer x:Name="SettingsView" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" Background="{DynamicResource CanvasBrush}">
                <StackPanel Margin="28,24" MaxWidth="600" HorizontalAlignment="Left">
                    <TextBlock Text="{DynamicResource S.nav.settings}" Foreground="{DynamicResource TextPrim}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,18"/>

                    <!-- Interfejs -->
                    <Border Style="{DynamicResource Card}">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource S.set.iface}" Style="{DynamicResource CardHead}"/>
                            <TextBlock Text="{DynamicResource S.set.uiscale}" Foreground="{DynamicResource TextSec}" Margin="0,6,0,4"/>
                            <ui:TextBox x:Name="SetUiScale" Width="120" HorizontalAlignment="Left"/>
                            <TextBlock Text="{DynamicResource S.set.bardelay}" Foreground="{DynamicResource TextSec}" Margin="0,16,0,4"/>
                            <ui:TextBox x:Name="SetBarDelay" Width="120" HorizontalAlignment="Left"/>
                            <TextBlock Text="{DynamicResource S.set.theme}" Foreground="{DynamicResource TextSec}" Margin="0,16,0,4"/>
                            <ComboBox x:Name="SetTheme" Width="160" HorizontalAlignment="Left">
                                <ComboBoxItem Content="{DynamicResource S.theme.dark}" Tag="Dark"/>
                                <ComboBoxItem Content="{DynamicResource S.theme.light}" Tag="Light"/>
                                <ComboBoxItem Content="{DynamicResource S.theme.system}" Tag="System"/>
                            </ComboBox>
                            <TextBlock Text="{DynamicResource S.set.language}" Foreground="{DynamicResource TextSec}" Margin="0,16,0,4"/>
                            <ComboBox x:Name="SetLanguage" Width="160" HorizontalAlignment="Left">
                                <ComboBoxItem Content="Polski" Tag="pl"/>
                                <ComboBoxItem Content="English" Tag="en"/>
                            </ComboBox>
                            <TextBlock Text="{DynamicResource S.set.liststyle}" Foreground="{DynamicResource TextSec}" Margin="0,16,0,4"/>
                            <ComboBox x:Name="SetListStyle" Width="160" HorizontalAlignment="Left">
                                <ComboBoxItem Content="{DynamicResource S.liststyle.default}" Tag="Default"/>
                                <ComboBoxItem Content="{DynamicResource S.liststyle.minimal}" Tag="Minimal"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>

                    <!-- Połączenie -->
                    <Border Style="{DynamicResource Card}">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource S.set.conn}" Style="{DynamicResource CardHead}"/>
                            <TextBlock Text="{DynamicResource S.set.port}" Foreground="{DynamicResource TextSec}" Margin="0,6,0,4"/>
                            <ui:TextBox x:Name="SetDefaultPort" Width="120" HorizontalAlignment="Left"/>
                            <TextBlock Text="{DynamicResource S.set.colordepth}" Foreground="{DynamicResource TextSec}" Margin="0,16,0,4"/>
                            <ComboBox x:Name="SetColorDepth" Width="120" HorizontalAlignment="Left">
                                <ComboBoxItem>16</ComboBoxItem>
                                <ComboBoxItem>24</ComboBoxItem>
                                <ComboBoxItem>32</ComboBoxItem>
                            </ComboBox>
                            <CheckBox x:Name="SetAutoReconnect" Content="{DynamicResource S.set.autoreconnect}" Foreground="{DynamicResource TextSec}" Margin="0,16,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Zachowanie -->
                    <Border Style="{DynamicResource Card}">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource S.set.behavior}" Style="{DynamicResource CardHead}"/>
                            <CheckBox x:Name="SetReachEnabled" Content="{DynamicResource S.set.reach}" Foreground="{DynamicResource TextSec}" Margin="0,6,0,0"/>
                            <TextBlock Text="{DynamicResource S.set.reachinterval}" Foreground="{DynamicResource TextSec}" Margin="0,10,0,4"/>
                            <ui:TextBox x:Name="SetReachInterval" Width="120" HorizontalAlignment="Left"/>
                            <CheckBox x:Name="SetConfirmClose" Content="{DynamicResource S.set.confirmclose}" Foreground="{DynamicResource TextSec}" Margin="0,16,0,0"/>
                            <CheckBox x:Name="SetConnLog" Content="{DynamicResource S.set.connlog}" Foreground="{DynamicResource TextSec}" Margin="0,10,0,0"/>
                            <CheckBox x:Name="SetOpenNewWindow" Content="{DynamicResource S.set.opennewwin}" Foreground="{DynamicResource TextSec}" Margin="0,10,0,0"/>
                            <CheckBox x:Name="SetImmersive" Content="{DynamicResource S.set.immersive}" Foreground="{DynamicResource TextSec}" Margin="0,10,0,0"/>
                            <CheckBox x:Name="SetCheckUpdates" Content="{DynamicResource S.set.checkupdates}" Foreground="{DynamicResource TextSec}" Margin="0,10,0,0"/>
                            <CheckBox x:Name="SetMinimizeToTray" Content="{DynamicResource S.set.traymin}" Foreground="{DynamicResource TextSec}" Margin="0,10,0,0"/>
                            <CheckBox x:Name="SetHotkey" Content="{DynamicResource S.set.hotkey}" Foreground="{DynamicResource TextSec}" Margin="0,10,0,0"/>
                            <CheckBox x:Name="SetRestorePrompt" Content="{DynamicResource S.set.restoreprompt}" Foreground="{DynamicResource TextSec}" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Połącz na starcie -->
                    <Border Style="{DynamicResource Card}">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource S.set.autoconnect}" Style="{DynamicResource CardHead}"/>
                            <TextBlock Text="{DynamicResource S.set.autoconnect.hint}" Foreground="{DynamicResource TextTer}"
                                       FontSize="11.5" TextWrapping="Wrap" Margin="0,0,0,8"/>
                            <Border BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="8" Padding="10,6">
                                <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                    <StackPanel x:Name="AutoConnectList"/>
                                </ScrollViewer>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Dane -->
                    <Border Style="{DynamicResource Card}">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource S.set.data}" Style="{DynamicResource CardHead}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                <TextBlock x:Name="SetDataPath" Foreground="{DynamicResource TextTer}" FontFamily="{DynamicResource Mono}"
                                           FontSize="11.5" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="380"/>
                                <ui:Button Content="{DynamicResource S.set.openfolder}" Appearance="Secondary" Click="OpenDataFolder_Click" Margin="12,0,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <ui:Button Content="{DynamicResource S.set.exportprofile}" Appearance="Secondary" Click="ExportProfile_Click"/>
                                <ui:Button Content="{DynamicResource S.set.importprofile}" Appearance="Secondary" Click="ImportProfile_Click" Margin="8,0,0,0"/>
                            </StackPanel>
                            <TextBlock Foreground="{DynamicResource TextTer}" FontSize="11" TextWrapping="Wrap" Margin="0,6,0,0"
                                       Text="{DynamicResource S.set.backupnote}"/>
                        </StackPanel>
                    </Border>

                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <ui:Button Content="{DynamicResource S.set.save}" Appearance="Primary" Click="SaveSettings_Click"/>
                        <TextBlock x:Name="SettingsStatus" Foreground="{DynamicResource Online}" VerticalAlignment="Center" Margin="14,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
            </Grid>
        </Grid>

        <!-- Szybkie przełączanie z paska kart (tryb skupienia): popup = osobny HWND, rysuje się nad sesją -->
        <Popup x:Name="QuickSwitchPopup" Placement="Bottom" PlacementTarget="{Binding ElementName=QuickSwitchBtn}"
               StaysOpen="False" AllowsTransparency="True">
            <Border Background="{DynamicResource RailBg}" CornerRadius="10" Padding="8" Margin="0,4,12,12"
                    BorderBrush="{DynamicResource Border}" BorderThickness="1" Width="300">
                <Border.Effect><DropShadowEffect BlurRadius="24" ShadowDepth="6" Opacity="0.45"/></Border.Effect>
                <StackPanel>
                    <ui:TextBox x:Name="QuickSwitchSearch" PlaceholderText="{DynamicResource S.search}"
                                Margin="0,0,0,6" TextChanged="QuickSwitchSearch_TextChanged" KeyDown="QuickSwitchSearch_KeyDown">
                        <ui:TextBox.Icon><ui:SymbolIcon Symbol="Search24"/></ui:TextBox.Icon>
                    </ui:TextBox>
                    <ScrollViewer MaxHeight="340" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource S.fs.opensessions}" Foreground="{DynamicResource TextTer}" FontSize="10"
                                       FontWeight="SemiBold" Margin="6,6,6,4"/>
                            <StackPanel x:Name="QsSessions"/>
                            <TextBlock Text="{DynamicResource S.fs.allservers}" Foreground="{DynamicResource TextTer}" FontSize="10"
                                       FontWeight="SemiBold" Margin="6,8,6,4"/>
                            <StackPanel x:Name="QsServers"/>
                        </StackPanel>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Popup>

        <!-- Lewy panel w trybie skupienia. Popup = osobny HWND, więc nakłada się na sesję (RDP/WebView2)
             BEZ zmiany jej rozmiaru — sesja się nie renegocjuje i nie miga. Rail+Sidebar są tu przenoszone
             na czas peeka (reparenting), a płynne wysunięcie robi animacja TranslateTransform. -->
        <Popup x:Name="FocusPeekPopup" Placement="Relative" PlacementTarget="{Binding ElementName=BodyGrid}"
               AllowsTransparency="True" StaysOpen="True" HorizontalOffset="0" VerticalOffset="0">
            <Border x:Name="FocusPeekClip" ClipToBounds="True" Width="280">
                <StackPanel x:Name="FocusPeekHost" Orientation="Horizontal" HorizontalAlignment="Left">
                    <StackPanel.RenderTransform>
                        <TranslateTransform x:Name="FocusPeekSlide" X="-280"/>
                    </StackPanel.RenderTransform>
                </StackPanel>
            </Border>
        </Popup>

        <!-- floating fullscreen bar (Popup = osobny HWND -> rysuje się nad kontrolką RDP) -->
        <Popup x:Name="FsPopup" Placement="Custom" PlacementTarget="{Binding ElementName=HotZone}"
               AllowsTransparency="True" StaysOpen="True" MouseLeave="FsPopup_MouseLeave">
            <Border Background="{DynamicResource FsBarBg}" CornerRadius="10" Padding="14,7" Margin="12,4,12,12"
                    BorderBrush="{DynamicResource Border}" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="8" Opacity="0.5"/>
                </Border.Effect>
                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Horizontal">
                        <Thumb x:Name="FsBarThumb" Width="20" Cursor="SizeWE" DragDelta="FsBarThumb_DragDelta"
                               Margin="0,0,10,0" ToolTip="{DynamicResource S.fs.drag}">
                            <Thumb.Template>
                                <ControlTemplate TargetType="Thumb">
                                    <Border Background="Transparent">
                                        <TextBlock Text="⠿" FontSize="15" Foreground="{DynamicResource TextTer}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Thumb.Template>
                        </Thumb>
                        <TextBlock x:Name="FsName" Text="—" Foreground="{DynamicResource TextPrim}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <Border Width="1" Background="{DynamicResource Border}" Margin="12,2"/>
                        <ui:Button x:Name="InnyBtn" Content="{DynamicResource S.fs.others}" Appearance="Secondary" Click="ToggleFlyout_Click"/>
                        <Border Width="1" Background="{DynamicResource Border}" Margin="12,2"/>
                        <ui:Button x:Name="PinBtn" Content="{DynamicResource S.fs.pin}" Appearance="Secondary" Click="TogglePin_Click"/>
                        <Border Width="1" Background="{DynamicResource Border}" Margin="12,2"/>
                        <Button Style="{DynamicResource IconBtn}" Click="Minimize_Click" ToolTip="{DynamicResource S.tip.minimize}">
                            <TextBlock Text="&#x2212;" FontSize="15" Foreground="{DynamicResource TextSec}" VerticalAlignment="Center"/>
                        </Button>
                        <ui:Button Content="{DynamicResource S.fs.restore}" Appearance="Secondary" Click="Fullscreen_Click" Margin="6,0,0,0"/>
                    </StackPanel>

                    <!-- flyout "inne połączenia" -->
                    <Border x:Name="FsFlyout" Visibility="Collapsed" Width="300" Margin="0,8,0,0"
                            Background="{DynamicResource FsFlyoutBg}" CornerRadius="8" Padding="8">
                        <StackPanel>
                            <ui:TextBox x:Name="FsFlyoutSearch" PlaceholderText="{DynamicResource S.search}"
                                        Margin="0,0,0,6" TextChanged="FsFlyoutSearch_TextChanged">
                                <ui:TextBox.Icon><ui:SymbolIcon Symbol="Search24"/></ui:TextBox.Icon>
                            </ui:TextBox>
                            <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource S.fs.opensessions}" Foreground="{DynamicResource TextTer}" FontSize="10"
                                               FontWeight="SemiBold" Margin="6,6,6,4"/>
                                    <StackPanel x:Name="FlyoutSessions"/>
                                    <TextBlock Text="{DynamicResource S.fs.allservers}" Foreground="{DynamicResource TextTer}" FontSize="10"
                                               FontWeight="SemiBold" Margin="6,8,6,4"/>
                                    <StackPanel x:Name="FlyoutServers"/>
                                </StackPanel>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</ui:FluentWindow>
